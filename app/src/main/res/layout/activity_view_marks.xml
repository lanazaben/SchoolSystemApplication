<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/beige"
    tools:context=".ViewMarks">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        style="@style/CustomToolbar"
        app:title="Student Marks"
        app:navigationIcon="@drawable/profile"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <ImageView
        android:id="@+id/backArrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/baseline_arrow_back_24"
        android:contentDescription="@string/back"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_margin="8dp" />


    <HorizontalScrollView
        android:id="@+id/tableFrame"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_margin="16dp"
        app:layout_constraintTop_toBottomOf="@id/backArrow"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <ScrollView
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:padding="8dp">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical">
                <TextView
                    android:id="@+id/subjectTextView"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/subject_name"
                    android:textSize="24sp"
                    android:textStyle="bold"
                    android:textColor="@color/blue"
                    android:layout_gravity="center"
                    android:layout_marginTop="12dp"
                    android:layout_marginBottom="16dp"
                    android:paddingHorizontal="24dp"
                    android:paddingVertical="12dp"
                    android:background="@drawable/animatedback"
                    android:elevation="4dp" />


                <TableLayout
                    android:id="@+id/marksTable"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:stretchColumns="*"
                    android:background="@drawable/cell_border">

                    <!-- Header Row -->
                    <TableRow android:background="@color/beige">
                        <TextView android:text="@string/exam" android:padding="8dp" android:textStyle="bold" android:background="@drawable/cell_border" />
                        <TextView android:text="@string/type" android:padding="8dp" android:textStyle="bold" android:background="@drawable/cell_border" />
                        <TextView android:text="@string/max" android:padding="8dp" android:textStyle="bold" android:background="@drawable/cell_border" />
                        <TextView android:text="@string/grade" android:padding="8dp" android:textStyle="bold" android:background="@drawable/cell_border" />
                        <TextView android:text="@string/min" android:padding="8dp" android:textStyle="bold" android:background="@drawable/cell_border" />
                        <TextView android:text="@string/date" android:padding="8dp" android:textStyle="bold" android:background="@drawable/cell_border" />
                    </TableRow>

                </TableLayout>
            </LinearLayout>
        </ScrollView>
    </HorizontalScrollView>
</androidx.constraintlayout.widget.ConstraintLayout>
